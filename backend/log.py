import logging

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# messages
msg_connection_db = " Successfully connected to database"
msg_close_db = " Connection to the database closed"